# TripWhisper 团队协作约定

## 事实来源与产品边界

- 已实现产品的模块、交互与业务规则以 [`docs/product/`](./docs/product/README.md) 为活文档。根目录 `README.md` 是开发者快速说明；讨论稿（`产品方案与开发路线.html`、`docs/第一步_产品需求定义_v0.1.md`）只说明方向和验收设想，不得写成线上真实功能。
- 合并会改变用户可见行为或业务规则的 PR，须在同一 PR 内更新 `docs/product/`（或写明「无行为变化，文档未改」）。
- 当前优先完成 P0 旅行工作台闭环：建立旅行、生成和检查行程、保护已订项目、受约束的调整、清晰展示差异和版本恢复。
- 没有经过验证的数据来源时，不得声称价格、库存、营业时间、路线或模型生成结果为实时、确定的信息。

## Git 与 Pull Request

- 每次改动都从最新的 `main` 开始。不得直接向 `main` 提交或推送。
- 每项实现改动都必须有对应的 GitHub Issue，其中写明用户目标、范围和验收条件。
- 一个 Issue 对应一个分支和一个 Pull Request。建议命名：`feature/<issue编号>-<简短名称>`、`fix/<issue编号>-<简短名称>` 或 `docs/<issue编号>-<简短名称>`。
- 每个 PR 应聚焦单一主题；不要混入无关的格式调整、重构或新增依赖。
- 请求审阅前，必须说明改了什么、刻意未改什么、验证证据，以及是否影响部署。
- 合并前等待另一位成员审阅和批准。未经明确同意，不得强制推送、重写共享历史或合并另一位成员的 PR。
- 避免两人同时修改同一个功能区域；如无法避免，编码前先在 Issue 中约定文件归属。

## 验证

- 修改应用代码并创建 PR 前，运行：
  - `npm run lint`
  - `npx tsc --noEmit`
  - `node --experimental-strip-types tests/journey.test.mjs`
  - `node tests/workspace.test.mjs`
  - `npm run build`
- 在 PR 中说明任何未运行的命令和原因；不得把未运行的检查描述为已通过。
- 修改的用户流程需要浏览器测试时，使用 `npm run dev` 进行本地人工验证。

## 安全与发布

- 不得提交 `.env`、令牌、API 密钥、可识别用户身份的旅行数据或生产环境凭据。后续接入时，密钥须仅保存在服务端。
- 合并到 `main` 不等于线上站点已经更新。PR 必须注明是否需要部署；由指定发布人员记录已部署的提交，并在发布后验证核心流程。
- 未经两位协作者明确同意，不得修改部署配置、破坏性 Git 状态或生产数据。
